import './theme.js';import {db} from './firebase.js';import {subscribeAuth,requireAuth,currentProfile} from './auth.js';import {getLatestPosts,getNotifications} from './services.js';import { $, escapeHtml, initials, timeAgo, toast } from './utils.js';
function postCard(p){return `<article class="card post-card compact-post"><div class="post-head"><div class="avatar">${p.photoURL?`<img src="${escapeHtml(p.photoURL)}" alt="">`:initials(p.authorName)}</div><div class="post-meta"><strong>${escapeHtml(p.authorName||'Campus Student')}</strong><span>${escapeHtml(p.branch||'Student')} · ${timeAgo(p.createdAt)}</span></div></div><div class="post-text">${escapeHtml(p.text||'')}</div><div class="post-actions"><span class="action"><i data-lucide="heart"></i>${p.likes||0}</span><span class="action"><i data-lucide="message-circle"></i>${p.comments||0}</span><a class="action" href="feed.html">Open feed <i data-lucide="arrow-up-right"></i></a></div></article>`}
function shellData(){const events=[['SEP','18','Campus Hacknight','Main Lab · 5:30 PM'],['SEP','23','Freshers Connect','Auditorium · 4:00 PM'],['OCT','02','Design Meetup','Innovation Lab · 3:00 PM']];$('[data-event-list]').innerHTML=events.map(e=>`<div class="list-row"><div class="row"><div class="event-date"><span class="small">${e[0]}</span><strong>${e[1]}</strong></div><div><strong>${e[2]}</strong><div class="small muted">${e[3]}</div></div></div></div>`).join('')}
async function load(){const root=$('[data-home-content]');let posts=[];try{posts=await getLatestPosts(3)}catch(e){toast('Could not load the live feed. Showing a starter view.','error')}if(!posts.length)posts=[{authorName:'Campus Connect',branch:'Community',text:'Welcome to Campus Connect. Share updates, join communities, discover events and raise campus issues safely.',likes:12,comments:4,createdAt:new Date()}];root.innerHTML=`<div class="section-title"><div><div class="kicker">Community pulse</div><h2>Trending discussions</h2></div><a class="muted-link" href="feed.html">View all <i data-lucide="arrow-right"></i></a></div><div class="stack">${posts.map(postCard).join('')}</div><div class="section-title"><div><div class="kicker">Personalized</div><h2>Made for your campus</h2></div></div><div class="grid grid-3"><a class="card feature-link" href="grouplist.html"><span class="feature-icon"><i data-lucide="users-round"></i></span><strong>Find communities</strong><span class="small muted">Groups around study, coding, clubs and more.</span></a><a class="card feature-link" href="issues.html"><span class="feature-icon"><i data-lucide="shield-plus"></i></span><strong>Raise an issue</strong><span class="small muted">Publicly anonymous, privately accountable.</span></a><a class="card feature-link" href="events.html"><span class="feature-icon"><i data-lucide="calendar-days"></i></span><strong>See events</strong><span class="small muted">Keep up with what is happening on campus.</span></a></div>`;window.lucide?.createIcons();shellData()}
subscribeAuth((user,profile)=>{if(!requireAuth())return;const hour=new Date().getHours();$('[data-greeting]').textContent=`Good ${hour<12?'morning':hour<18?'afternoon':'evening'}, ${profile?.displayName?.split(' ')[0]||'there'}`;load()});
